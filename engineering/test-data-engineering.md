# Test Data Engineering

Reliable automation depends on data that is isolated, discoverable, representative, and disposable.

## Practices

Prefer builders and APIs over brittle shared fixtures. Generate unique identifiers for parallel work, reset or clean up state deliberately, and keep sensitive data synthetic or masked. Document ownership, lifecycle, dependencies, and environment constraints for important datasets.

Data failures should be distinguishable from product failures in reports.

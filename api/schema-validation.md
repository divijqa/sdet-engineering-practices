# Schema Validation

Schema validation checks that API responses and requests conform to an agreed structural contract.

Validate required and optional fields, types, formats, enums, nesting, additional properties, and error payloads. Keep schemas versioned with the API and test representative success, boundary, and failure responses.

A schema pass does not prove business correctness; pair it with semantic assertions and persistence or downstream checks where needed.
